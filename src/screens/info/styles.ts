import { StyleSheet } from 'react-native';

import Constants from '../../const';

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flex: 1,
    backgroundColor: Constants.Colors.Ferrari01,
  },
});

export default styles;
